{
  "name": "pet-collar-qr",
  "private": true,
  "scripts": {
    "dev": "concurrently -n back,front -c blue,green \"npm run dev:back\" \"npm run dev:front\"",
    "dev:back": "cd backend && uvicorn main:app --reload --host 0.0.0.0",
    "dev:front": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
