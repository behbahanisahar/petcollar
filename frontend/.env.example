# Optional: Backend API URL. If not set, uses same origin (for local dev or same-domain setup).
# For Vercel deployment, set this in Vercel env vars to your Railway backend URL.
# VITE_API_URL=http://localhost:8000
